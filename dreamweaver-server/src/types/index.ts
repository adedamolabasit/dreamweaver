export interface CreateJournalParams {
  userId: string;
  transcript: string;
}

export interface CreateJournalParams {
  userId: string;
  transcript: string;
}

export interface UpdateJournalParams {
  transcript: string;
}

